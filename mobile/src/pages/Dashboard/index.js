import React from 'react';
import { View, Text } from 'react-native';

function Dashboard() {
    return (
        <View>
            <Text>Dashboard</Text>
        </View>
    );
};

export default Dashboard;