import React from 'react';
import { View } from 'react-native';

function Header() {
    return (
        <View
            style={{
                height: 60,
                width: '100%',
                backgroundColor: '#ff0000'
            }}
        ></View>
    )
}

export default Header;